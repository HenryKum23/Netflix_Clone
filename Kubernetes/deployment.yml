apiVersion: apps/v1
kind: Deployment
metadata:
  name: netflix
  labels:
    app: netflix-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: netflix-app
  template:
    metadata:
      labels:
        app: netflix-app
    spec:
      containers:
      - name: netflixcc
        image: henrykum23/netflix:latest
        ports:
        - containerPort: 80
